# 本地运行与测试指南

## 1. 直接启动

双击项目根目录的 `index.html`。游戏会从 `vendor/` 加载 Phaser，并从 `assets/offline.js` 读取图片和音频，因此无需联网或本地服务。首次点击页面或按键后浏览器才允许播放声音，这是浏览器的自动播放规则。

## 2. 使用 HTTP 服务调试

安装 Python 3，在包含 `index.html` 的项目根目录执行以下命令并保持终端窗口打开：

```powershell
python -m http.server 8080 --bind 127.0.0.1
```

如果提示找不到 `python`，Windows 可以尝试：

```powershell
py -m http.server 8080 --bind 127.0.0.1
```

出现 `Serving HTTP on 127.0.0.1 port 8080` 表示服务已启动。

## 3. 测试内容

- 手机横屏和竖屏进入游戏，确认底部出现冲刺按钮且不覆盖画布；点击按钮应只冲刺，不触发上升。冷却、暂停、粘液禁冲刺时按钮不可用；返回菜单后隐藏。桌面端不显示该按钮，空格仍可冲刺。
- 在角色下落时冲刺，确认本体和碰撞箱一起前移，冲刺期间高度不变；残影停留在身后并淡出，结束后恢复下落。

- [游戏页面](http://127.0.0.1:8080/index.html)：测试选角、飞行、冲刺和场景效果。
- 主界面点击左下角「新手教程」，确认只显示一张机制图，明确说明冲刺能撞碎绿色藤蔓和敌人，但不能撞碎石墙与浮岛。
- [十群系美术预览](http://127.0.0.1:8080/art-preview.html)：点击群系按钮切换场景，可显示碰撞框、关闭背景滚动；展开「资源与行为验证」查看检查结果。
- 点击预览页的「幻翼从左侧俯冲」或「幻翼从右侧俯冲」，可观察实际轨迹是否穿过玩家列的中段；蜜蜂与发光鱿鱼已放大两倍，可同时检查新的细长障碍和墙面。

### 本轮鹦鹉与音频检查

1. 游戏主菜单进入选角，检查蓝、绿、红三只鹦鹉交替展翅／下拍；贴图宽高已扩大 1.6 倍，角色手感与碰撞框保持原值。预览页下方三色鹦鹉也会循环拍翼。
2. 点击页面后才会启动音乐，这是浏览器正常的自动播放限制。
3. 拖动主菜单右上角两个音量条；点击右下角「⚙ 设置」分别关闭／开启背景音乐与音效。刷新后确认音量与开关状态保留。
4. 游戏中打开设置应暂停；选角过场中打开设置也应冻结过场，关闭才继续。
5. [美术预览](http://127.0.0.1:8080/art-preview.html) 下方新增三色鹦鹉及音频试听。点击音效按钮试听，点击群系按钮对比同一首音乐的音色变化。
6. 实机中验证：成功冲刺播冲刺音效、扣血播受伤、失败播一次失败音效、绿宝石播收集；冲刺击毁直升机播 `man.mp3`，其他敌人不播此音效。确认绿宝石和生命值拾取物显示新 16×16 贴图。
7. 无声音时检查音量条、设置开关、系统音量和浏览器标签页静音。

### 本轮玩法调整检查

1. 主界面标题显示为「像素飘流」，成功冲刺时画面不抖动。
2. 直升机和蝙蝠的显示尺寸分别为 57.6×43.2、43.2×28.8；直升机碰撞箱同步放大，冲刺更容易命中。
3. 平原、海边、森林的障碍生成明显更疏，之后三个群系的生成参数保持不变。
4. 连续拾取生命道具时，生命上限最多增长到 6。

自动检查：`node --test tests/*.cjs` 和 `node tools/pack-assets.cjs --check`。

修改 `assets/art/` 或运行时音频后，执行 `node tools/pack-assets.cjs` 重新生成 `assets/offline.js`，再提交两者。

### 新增四群系检查

在美术预览页依次点击「末地」「雪林」「花开尖塔」「哭嚎炼狱」，检查背景、墙面、地面、细长障碍和浮岛均同步切换，没有黑底或缺图。展开「资源与行为验证」，应全部通过。

正式游戏仍按原来的群系时长自动切换；玄武岩三角洲之后为末地 → 雪林 → 花开尖塔 → 哭嚎炼狱，之后回到平原。无需等待整轮即可用预览页检查美术。新增群系沿用既有敌人与障碍生成配置。

新增资源已打入 `assets/offline.js`，也可以双击 `art-preview.html` 验证离线图片加载。

本轮回归发现一项原有测试约定不一致：当前已提交的玩法允许生命水晶无限增长，但 `tests/audio-game.cjs` 和 `tests/gameplay-tuning.cjs` 各有一项仍断言上限为 6。这两项会失败；本次按「不修改其它机制」要求保留现有玩法和测试。新增群系资源、切换顺序、离线包与原有怪物轨迹检查独立通过。

本轮已在 HTTP 预览中逐一检查四个新场景，91 个纹理全部加载，22 项运行时检查通过，无控制台错误；也检查了四张背景循环接缝以及障碍与浮岛的透明度。自动浏览器的安全策略禁止访问 `file://`，因此双击启动的视觉测试请由团队手动复查；离线素材完整性与离线包同步检查已通过。

### 新敌人逻辑测试

打开 [敌人测试页](http://127.0.0.1:8080/enemy-preview.html)。本页使用正式像素 PNG 与动画，自机固定位置便于观察。

1. 点击「四向潜影贝」：观察贴附偏移保持不变、开合提示及粘液射击。
2. 点击「粘液命中」，再点击「尝试冲刺」：生命减少、禁冲刺倒计时约 3 秒，期间冲刺被阻止，归零后可再次冲刺。
3. 点击「地狱水母」，立即点击「冲刺击毁当前水母／史莱姆」：右前方出现 4–6 颗火球。
4. 点击「史莱姆落地」或「史莱姆左边界」：触及对应边界时向右上方散弹；重新生成后立即冲刺击毁，应无散弹。
5. 正式游戏中，潜影贝在末地／花开尖塔随墙生成，水母在哭嚎炼狱生成，史莱姆在花开尖塔生成。

自动检查：`node tests/new-enemies.cjs`。新增 10 张 PNG 已打入离线包；修改素材后执行 node tools/pack-assets.cjs 更新。

## 4. 停止服务

回到启动服务的终端，按 **Ctrl+C** 即可停止。

## 常见问题

- **端口 8080 被占用**：把命令里的 `8080` 改为 `8081`，浏览器地址中的端口也改为 `8081`。
- **页面显示文件列表或 404**：检查启动命令所在目录，必须是包含 `index.html` 的项目根目录。
- **修改后仍显示旧素材**：按 **Ctrl+F5** 强制刷新页面。
- **页面空白**：确认 `vendor/phaser.min.js` 与 `assets/offline.js` 存在且与 `index.html` 保持原有相对目录。
- **同事电脑无法访问你的地址**：`127.0.0.1` 仅指向当前电脑；每位成员取得项目文件后，在自己的电脑上按上述步骤启动即可。
